providers = ["python"]

[phases.setup]
nixpkgs_archive = "pkgs/unstable"

[phases.install]
src_dir = "."
onlyDeps = false

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmds = ["python -m uvicorn main:app --host 0.0.0.0 --port ${PORT}"]
